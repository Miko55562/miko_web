import React from 'react';

const Main = () => {
  return (
    <div style={mainStyle}>
    </div>
  );
};

const mainStyle = {
  height: "100vh",
  font: "Fira Sans",
  background: '#333',
  color: '#111',
  textAlign: 'left',
  padding: '1rem',
};

export default Main;
